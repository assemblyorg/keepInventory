# keepInventory API module
This module is responsible for the plugin's API. To work with this module, follow these instructions:

#### Add repository:
```kotlin
repositories {
    maven("https://jitpack.io")
}
```
#### Add dependency:
```kotlin
dependencies {
    implementation("com.github.assemblyDir.keepInventory:api:1.0.0")
}
```